<!--
  Full guide: docs/pr-descriptions.md
  Delete any section that doesn't apply. Delete these comments.
-->

## Problem

<!-- Why this exists. Concrete and falsifiable — the measurement, the broken
     behaviour, the capability that was missing. A reviewer should be able to
     disagree with the premise here, before reading a line of the diff. -->

## Change

<!-- What the diff does NOT already say: the approach, and the reasoning behind
     it. If an obvious simpler fix doesn't work, say why it doesn't. Cite
     `path/to/file.ext:line`. Tables for before/after values, short snippets for
     the load-bearing few lines. -->

## Notes

<!-- Optional. Trade-offs, compatibility, deliberate scope boundaries.
     For shader/visual work, state explicitly whether existing designs render
     identically at default values. -->

## Not done here

<!-- Optional, but required if anything is deferred: manual admin steps, secrets
     to provision, follow-up work, known gaps. Anything merging would silently
     assume. -->

## Testing

<!-- The command and its actual result, with counts: `npm test` — 569 passed,
     19 files. Separate automated from visual. If local visual verification is
     still outstanding, say so plainly — never imply it happened. Screenshots or
     GIFs for anything that renders. -->
