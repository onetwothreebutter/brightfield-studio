<section class="shader-demo" id="shader-demo">
  <div class="shader-demo__header container">
    {%- if section.settings.eyebrow != blank -%}
      <p class="shader-demo__eyebrow">{{ section.settings.eyebrow }}</p>
    {%- endif -%}
    <h2 class="shader-demo__heading">{{ section.settings.heading }}</h2>
    {%- if section.settings.subheading != blank -%}
      <p class="shader-demo__subheading">{{ section.settings.subheading }}</p>
    {%- endif -%}
  </div>

  <div class="shader-demo__body container">
    <div class="shader-demo__canvas-wrap">
      <canvas id="demo-shader-canvas" class="shader-demo__canvas" aria-hidden="true"></canvas>
    </div>

    <div class="shader-demo__controls">

      <div class="scroll-shadow scroll-shadow--top" aria-hidden="true"></div>

      <!-- Randomize / Copy Link -->
      <button type="button" class="shader-control__randomize-btn" id="demo-randomize">Randomize</button>
      <button type="button" class="shader-control__copy-link-btn" id="demo-copy-link">Copy Link</button>

      <!-- Cycle indicator dots -->
      <div class="shader-demo__cycle-indicator" id="demo-cycle-dots" aria-hidden="true"></div>

      <!-- Color Palette -->
      <div class="shader-control">
        <label class="shader-control__label">Color Palette</label>
        <select class="shader-control__select" id="demo-preset">
          <optgroup label="Cosine">
            <option value="Rainbow">Rainbow</option>
            <option value="Cool Blue">Cool Blue</option>
            <option value="Neon Heat">Neon Heat</option>
            <option value="Golden">Golden</option>
            <option value="Retro">Retro</option>
          </optgroup>
          <optgroup label="4-Color">
            <option value="Neon">Neon</option>
            <option value="Sunset">Sunset</option>
            <option value="Aurora">Aurora</option>
            <option value="Dusk">Dusk</option>
            <option value="Retro 4C">Retro</option>
          </optgroup>
        </select>
      </div>

      <!-- Lines -->
      <div class="shader-control">
        <label class="shader-control__label">Lines</label>
        <div class="shader-control__range-wrap">
          <input type="range" class="shader-control__range" id="demo-line-count" min="5" max="60" step="1" value="20">
          <span class="shader-control__value" id="demo-line-count-display">20</span>
        </div>
      </div>

      <!-- Power -->
      <div class="shader-control">
        <label class="shader-control__label">Power</label>
        <div class="shader-control__range-wrap">
          <input type="range" class="shader-control__range" id="demo-power" min="0.5" max="6.0" step="0.1" value="2.5">
          <span class="shader-control__value" id="demo-power-display">2.5</span>
        </div>
      </div>

      <!-- Center Circle -->
      <div class="shader-control">
        <label class="shader-control__label">Center Circle</label>
        <div class="shader-control__range-wrap">
          <input type="range" class="shader-control__range" id="demo-center-circle" min="0.0" max="0.4" step="0.005" value="0.04">
          <span class="shader-control__value" id="demo-center-circle-display">0.04</span>
        </div>
      </div>

      <!-- All Settings (collapsible) -->
      <div class="shader-demo__all-settings">
        <button type="button" class="shader-demo__all-settings-toggle" id="demo-all-settings-toggle" aria-expanded="false">
          All Settings
          <span class="shader-demo__all-settings-chevron" aria-hidden="true">&#9660;</span>
        </button>
        <div class="shader-demo__all-settings-body" id="demo-all-settings-body" hidden>

          <p class="shader-demo__panel-label">Shape</p>

          <!-- Radius -->
          <div class="shader-control">
            <label class="shader-control__label">Radius</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-radius" min="0.05" max="0.5" step="0.01" value="0.31">
              <span class="shader-control__value" id="demo-radius-display">0.31</span>
            </div>
          </div>

          <!-- Width Top -->
          <div class="shader-control">
            <label class="shader-control__label">Width Top</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-width-top" min="0" max="1" step="0.01" value="0.05">
              <span class="shader-control__value" id="demo-width-top-display">0.05</span>
            </div>
          </div>

          <!-- Width Bottom -->
          <div class="shader-control">
            <label class="shader-control__label">Width Bottom</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-width-bot" min="0" max="1" step="0.01" value="0.75">
              <span class="shader-control__value" id="demo-width-bot-display">0.75</span>
            </div>
          </div>

          <p class="shader-demo__panel-label">Color Palette</p>

          <!-- Color Mode -->
          <div class="shader-control">
            <label class="shader-control__label">Color Mode</label>
            <div class="shader-control__with-help">
              <select class="shader-control__select" id="demo-color-mode">
                <option value="0">Cosine</option>
                <option value="1">4-Color</option>
              </select>
              <button type="button" class="shader-demo__help-btn" id="demo-cosine-help" aria-label="About cosine palette">?</button>
            </div>
          </div>

          <!-- Cosine Preset -->
          <div class="shader-control" id="demo-cosine-preset-row">
            <label class="shader-control__label">Preset</label>
            <select class="shader-control__select" id="demo-cosine-preset">
              <option value="Rainbow">Rainbow</option>
              <option value="Cool Blue">Cool Blue</option>
              <option value="Neon Heat">Neon Heat</option>
              <option value="Golden">Golden</option>
              <option value="Retro">Retro</option>
            </select>
          </div>

          <!-- 4-Color Preset -->
          <div class="shader-control" id="demo-stop-preset-row" style="display:none">
            <label class="shader-control__label">Preset</label>
            <select class="shader-control__select" id="demo-stop-preset">
              <option value="Neon">Neon</option>
              <option value="Sunset">Sunset</option>
              <option value="Aurora">Aurora</option>
              <option value="Dusk">Dusk</option>
              <option value="Retro 4C">Retro</option>
            </select>
          </div>

          <!-- Palette A -->
          <div class="shader-control" id="demo-palette-a-row">
            <label class="shader-control__label">Palette A</label>
            <input type="color" class="shader-control__color" id="demo-palette-a" value="#808080">
          </div>

          <!-- Palette B -->
          <div class="shader-control" id="demo-palette-b-row">
            <label class="shader-control__label">Palette B</label>
            <input type="color" class="shader-control__color" id="demo-palette-b" value="#808080">
          </div>

          <!-- Palette C -->
          <div class="shader-control" id="demo-palette-c-row">
            <label class="shader-control__label">Palette C</label>
            <input type="color" class="shader-control__color" id="demo-palette-c" value="#ffffff">
          </div>

          <!-- Palette D -->
          <div class="shader-control" id="demo-palette-d-row">
            <label class="shader-control__label">Palette D</label>
            <input type="color" class="shader-control__color" id="demo-palette-d" value="#0054ab">
          </div>

          <!-- Color 1 (Top) -->
          <div class="shader-control" id="demo-color0-row" style="display:none">
            <label class="shader-control__label">Color 1 (Top)</label>
            <input type="color" class="shader-control__color" id="demo-color0" value="#ff3366">
          </div>

          <!-- Color 2 -->
          <div class="shader-control" id="demo-color1-row" style="display:none">
            <label class="shader-control__label">Color 2</label>
            <input type="color" class="shader-control__color" id="demo-color1" value="#ffcc00">
          </div>

          <!-- Color 3 -->
          <div class="shader-control" id="demo-color2-row" style="display:none">
            <label class="shader-control__label">Color 3</label>
            <input type="color" class="shader-control__color" id="demo-color2" value="#00ccff">
          </div>

          <!-- Color 4 (Bot) -->
          <div class="shader-control" id="demo-color3-row" style="display:none">
            <label class="shader-control__label">Color 4 (Bot)</label>
            <input type="color" class="shader-control__color" id="demo-color3" value="#aa00ff">
          </div>

          <p class="shader-demo__panel-label">Triangle Mask</p>

          <!-- Triangle Enabled -->
          <div class="shader-control">
            <label class="shader-control__label">Enabled</label>
            <button type="button" class="shader-control__toggle is-on" id="demo-tri-enabled" data-on="1">On</button>
          </div>

          <!-- Triangle Rotation -->
          <div class="shader-control">
            <label class="shader-control__label">Rotation (°)</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-tri-rotation" min="0" max="360" step="1" value="0">
              <span class="shader-control__value" id="demo-tri-rotation-display">0</span>
            </div>
          </div>

          <!-- Triangle Size -->
          <div class="shader-control">
            <label class="shader-control__label">Size</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-tri-size" min="0.1" max="2.0" step="0.01" value="1.0">
              <span class="shader-control__value" id="demo-tri-size-display">1</span>
            </div>
          </div>

          <!-- Triangle Base Width -->
          <div class="shader-control">
            <label class="shader-control__label">Base Width (°)</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-tri-width" min="5" max="89" step="1" value="45">
              <span class="shader-control__value" id="demo-tri-width-display">45</span>
            </div>
          </div>

          <p class="shader-demo__panel-label">Center Circle</p>

          <!-- Center Circle Enabled -->
          <div class="shader-control">
            <label class="shader-control__label">Enabled</label>
            <button type="button" class="shader-control__toggle is-on" id="demo-center-circle-enabled" data-on="1">On</button>
          </div>

          <p class="shader-demo__panel-label">Text Overlay</p>

          <!-- Enable Text -->
          <div class="shader-control">
            <label class="shader-control__label">Enable Text</label>
            <button type="button" class="shader-control__toggle" id="demo-enable-text" data-on="0">Off</button>
          </div>

          <!-- Text -->
          <div class="shader-control">
            <label class="shader-control__label">Text</label>
            <input type="text" class="shader-control__text-input" id="demo-text" value="{{ 'now' | date: '%Y' }}" placeholder="Your text">
          </div>

          <!-- Font -->
          <div class="shader-control">
            <label class="shader-control__label">Font</label>
            <select class="shader-control__select" id="demo-text-font">
              <option value="Oswald">Oswald</option>
              <option value="Unbounded">Unbounded</option>
              <option value="Bricolage Grotesque">Bricolage Grotesque</option>
              <option value="DM Mono">DM Mono</option>
              <option value="Righteous">Righteous</option>
              <option value="Teko">Teko</option>
              <option value="Big Shoulders Display">Big Shoulders Display</option>
              <option value="Anton">Anton</option>
            </select>
          </div>

          <!-- Font Size -->
          <div class="shader-control">
            <label class="shader-control__label">Font Size</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-text-size" min="8" max="300" step="1" value="69">
              <span class="shader-control__value" id="demo-text-size-display">69</span>
            </div>
          </div>

          <!-- Text X -->
          <div class="shader-control">
            <label class="shader-control__label">Text X</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-text-x" min="0" max="1" step="0.01" value="0.5">
              <span class="shader-control__value" id="demo-text-x-display">0.5</span>
            </div>
          </div>

          <!-- Text Y -->
          <div class="shader-control">
            <label class="shader-control__label">Text Y</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-text-y" min="0" max="1" step="0.01" value="0.2">
              <span class="shader-control__value" id="demo-text-y-display">0.2</span>
            </div>
          </div>

          <!-- Custom Text Color -->
          <div class="shader-control">
            <label class="shader-control__label">Custom Color</label>
            <button type="button" class="shader-control__toggle" id="demo-use-text-color" data-on="0">Off</button>
          </div>

          <!-- Text Color -->
          <div class="shader-control" id="demo-text-color-row" style="display:none">
            <label class="shader-control__label">Text Color</label>
            <input type="color" class="shader-control__color" id="demo-text-color" value="#ffffff">
          </div>

          <!-- Outline -->
          <div class="shader-control">
            <label class="shader-control__label">Outline</label>
            <button type="button" class="shader-control__toggle" id="demo-outline-enabled" data-on="0">Off</button>
          </div>

          <!-- Outline Width -->
          <div class="shader-control" id="demo-outline-width-row" style="display:none">
            <label class="shader-control__label">Outline Width</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-outline-width" min="1" max="60" step="1" value="8">
              <span class="shader-control__value" id="demo-outline-width-display">8</span>
            </div>
          </div>

          <!-- Outline Color -->
          <div class="shader-control" id="demo-outline-color-row" style="display:none">
            <label class="shader-control__label">Outline Color</label>
            <input type="color" class="shader-control__color" id="demo-outline-color" value="#000000">
          </div>

          <p class="shader-demo__panel-label">Finish</p>

          <!-- Opacity -->
          <div class="shader-control">
            <label class="shader-control__label">Opacity</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-opacity" min="0" max="1" step="0.01" value="1">
              <span class="shader-control__value" id="demo-opacity-display">1</span>
            </div>
          </div>

          <!-- Distress -->
          <div class="shader-control">
            <label class="shader-control__label">Distress</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-distress" min="0" max="0.85" step="0.01" value="0">
              <span class="shader-control__value" id="demo-distress-display">0</span>
            </div>
          </div>

          <!-- Grain -->
          <div class="shader-control">
            <label class="shader-control__label">Grain</label>
            <div class="shader-control__range-wrap">
              <input type="range" class="shader-control__range" id="demo-distress-scale" min="10" max="300" step="1" value="80">
              <span class="shader-control__value" id="demo-distress-scale-display">80</span>
            </div>
          </div>

        </div>
      </div>

      <div class="scroll-shadow scroll-shadow--bottom" aria-hidden="true"></div>

    </div>

  </div>

  <div class="shader-demo__footer container">
    <hr class="shader-demo__divider">
    <div class="shader-demo__cta-row">
      <a href="{{ section.settings.product_url }}" class="btn btn--outline">
        {{ section.settings.button_label | default: 'Buy Original' }}
      </a>
      <button type="button" class="btn btn--primary" id="demo-preview-btn">
        Preview on Shirt
      </button>
    </div>
  </div>
</section>

<!-- Cosine palette explainer modal -->
<div class="shader-demo__help-modal shader-demo__help-modal--hidden" id="demo-cosine-modal" role="dialog" aria-modal="true" aria-label="About cosine palette">
  <div class="shader-demo__help-modal__backdrop" id="demo-cosine-backdrop"></div>
  <div class="shader-demo__help-modal__box">
    <div class="shader-demo__help-modal__header">
      <span class="shader-demo__help-modal__title">Cosine Palette</span>
      <button class="shader-demo__help-modal__close" id="demo-cosine-close" aria-label="Close">&times;</button>
    </div>
    <div class="shader-demo__help-modal__body">
      <p class="shader-demo__help-modal__intro">A cosine palette generates smooth, cyclical color progressions by oscillating each RGB channel independently using a cosine wave.</p>
      <p class="shader-demo__help-modal__formula"><code>color = A + B &middot; cos(2&pi;(C &middot; t + D))</code></p>
      <dl class="shader-demo__help-modal__defs">
        <dt>A &mdash; Palette A</dt>
        <dd>Brightness offset. Sets the center point that colors oscillate around.</dd>
        <dt>B &mdash; Palette B</dt>
        <dd>Amplitude. Controls the range of color swing — higher means more vivid output.</dd>
        <dt>C &mdash; Palette C</dt>
        <dd>Frequency. How many times the palette cycles across the gradient.</dd>
        <dt>D &mdash; Palette D</dt>
        <dd>Phase. Shifts the starting hue of the cycle.</dd>
      </dl>
    </div>
  </div>
</div>

<!-- Mockup modal (reuses product page classes) -->
<div class="mockup-modal mockup-modal--hidden" id="demo-mockup-modal" role="dialog" aria-modal="true" aria-label="Shirt preview">
  <div class="mockup-modal__backdrop" id="demo-mockup-backdrop"></div>
  <div class="mockup-modal__box">
    <div class="mockup-modal__header">
      <span class="mockup-modal__title">Preview on Shirt</span>
      <button class="mockup-modal__close" id="demo-mockup-close" aria-label="Close preview">&times;</button>
    </div>
    <div class="mockup-modal__body">
      <p id="demo-mockup-loading" style="text-align:center;padding:2rem;color:var(--color-text-muted)">Generating preview…</p>
      <img class="mockup-modal__img" id="demo-mockup-img" src="" alt="Shirt mockup" style="display:none">
      <p id="demo-mockup-error" style="display:none;text-align:center;padding:2rem;color:var(--color-text-muted)">Preview failed. Please try again.</p>
    </div>
    <div class="mockup-modal__footer" id="demo-mockup-footer" style="display:none">
      <div class="mockup-modal__size" id="demo-mockup-size-wrap">
        <label class="mockup-modal__size-label" for="demo-mockup-size">Size</label>
        <select id="demo-mockup-size" class="mockup-modal__size-select">
          <option value="">Loading…</option>
        </select>
      </div>
      <div class="mockup-modal__footer-actions">
        <a id="demo-mockup-download" class="btn btn--outline btn--sm" download="my-design.jpg">Download</a>
        <button id="demo-mockup-order" class="btn btn--primary btn--sm">Order This Design</button>
      </div>
    </div>
  </div>
</div>

<script>
(function () {
  // ── Preset data ────────────────────────────────────────────────────────────
  // Each preset is self-contained: mode 0 = cosine (a/b/c/d), mode 1 = 4-stop (c0–c3).
  var PRESETS = {
    'Rainbow':   { mode: 0, a: [0.5,  0.5,  0.5 ], b: [0.5,  0.5,  0.5 ], c: [1.0, 1.0, 1.0 ], d: [0.0,  0.33, 0.67]  },
    'Cool Blue': { mode: 0, a: [0.5,  0.5,  0.5 ], b: [0.5,  0.5,  0.5 ], c: [1.0, 1.0, 1.0 ], d: [0.263,0.416,0.557] },
    'Neon Heat': { mode: 0, a: [0.5,  0.5,  0.5 ], b: [0.5,  0.5,  0.5 ], c: [1.0, 1.0, 1.0 ], d: [0.3,  0.2,  0.2  ] },
    'Golden':    { mode: 0, a: [0.5,  0.5,  0.5 ], b: [0.5,  0.5,  0.5 ], c: [1.0, 1.0, 0.5 ], d: [0.8,  0.9,  0.3  ] },
    'Retro':     { mode: 0, a: [0.6,  0.5,  0.3 ], b: [0.4,  0.4,  0.3 ], c: [1.0, 0.7, 0.5 ], d: [0.0,  0.2,  0.4  ] },
    'Neon':      { mode: 1, c0: [1.0,  0.2,  0.4  ], c1: [1.0,  0.8,  0.0  ], c2: [0.0,  0.8,  1.0  ], c3: [0.667,0.0,  1.0  ] },
    'Sunset':    { mode: 1, c0: [1.0,  0.318,0.184 ], c1: [0.867,0.141,0.463 ], c2: [0.541,0.169,0.886 ], c3: [0.098,0.098,0.439 ] },
    'Aurora':    { mode: 1, c0: [0.0,  1.0,  0.533 ], c1: [0.0,  0.898,1.0   ], c2: [0.486,0.302,1.0   ], c3: [0.878,0.251,0.984 ] },
    'Dusk':      { mode: 1, c0: [1.0,  0.6,  0.4   ], c1: [1.0,  0.369,0.384 ], c2: [0.8,  0.169,0.369 ], c3: [0.459,0.227,0.533 ] },
    'Retro 4C':  { mode: 1, c0: [0.91, 0.286,0.114 ], c1: [0.788,0.627,0.188 ], c2: [0.176,0.541,0.369 ], c3: [0.102,0.153,0.267 ] },
  };

  // Auto-cycle sequence — each step specifies preset + lineCount + power + centerCircle
  var CYCLE_STEPS = [
    { preset: 'Rainbow',  lineCount: 20, power: 2.5, centerCircle: 0.04  },
    { preset: 'Cool Blue',lineCount: 30, power: 1.5, centerCircle: 0.12  },
    { preset: 'Sunset',   lineCount: 15, power: 3.5, centerCircle: 0.08  },
    { preset: 'Neon Heat',lineCount: 25, power: 2.0, centerCircle: 0.18  },
    { preset: 'Aurora',   lineCount: 20, power: 4.0, centerCircle: 0.06  },
    { preset: 'Golden',   lineCount: 35, power: 2.0, centerCircle: 0.14  },
    { preset: 'Dusk',     lineCount: 12, power: 3.0, centerCircle: 0.0   },
    { preset: 'Retro',    lineCount: 18, power: 2.8, centerCircle: 0.10  },
  ];
  var CYCLE_INTERVAL_MS = 3000;

  // ── State ──────────────────────────────────────────────────────────────────
  window._demoState = {
    values: {
      u_line_count:  20,
      u_power:       2.5,
      u_radius:      0.31,
      u_width_top:   0.05,
      u_width_bot:   0.75,
      u_color_mode:             0,
      u_tri_enabled:            1,
      u_tri_rotation:           0,
      u_tri_size:               1.0,
      u_tri_width:              45,
      u_center_circle_enabled:  1,
      u_center_circle_radius:   0.04,
      u_palette_a: [0.5, 0.5, 0.5], u_palette_b: [0.5, 0.5, 0.5],
      u_palette_c: [1.0, 1.0, 1.0], u_palette_d: [0.0, 0.33, 0.67],
      u_color0: [1.0, 0.2, 0.4],   u_color1: [1.0, 0.8, 0.0],
      u_color2: [0.0, 0.8, 1.0],   u_color3: [0.667, 0.0, 1.0],
      text: '', textFont: 'Oswald', textFontSize: 69,
      textX: 0.5, textY: 0.2,
      u_use_text_color: 0, u_text_color: [1.0, 1.0, 1.0],
      outlineEnabled: false, outlineWidth: 8, u_outline_color: [0.0, 0.0, 0.0],
    },
    dirty: true,
  };

  // ── DOM refs ───────────────────────────────────────────────────────────────
  var presetSel      = document.getElementById('demo-preset');
  var lineCountInput = document.getElementById('demo-line-count');
  var lineCountDisp  = document.getElementById('demo-line-count-display');
  var powerInput          = document.getElementById('demo-power');
  var powerDisp           = document.getElementById('demo-power-display');
  var centerCircleInput   = document.getElementById('demo-center-circle');
  var centerCircleDisp    = document.getElementById('demo-center-circle-display');
  var randomizeBtn        = document.getElementById('demo-randomize');
  var dotsWrap            = document.getElementById('demo-cycle-dots');
  var allSettingsToggle   = document.getElementById('demo-all-settings-toggle');
  var allSettingsBody     = document.getElementById('demo-all-settings-body');
  var radiusInput         = document.getElementById('demo-radius');
  var radiusDisp          = document.getElementById('demo-radius-display');
  var widthTopInput       = document.getElementById('demo-width-top');
  var widthTopDisp        = document.getElementById('demo-width-top-display');
  var widthBotInput       = document.getElementById('demo-width-bot');
  var widthBotDisp        = document.getElementById('demo-width-bot-display');
  var triEnabledBtn       = document.getElementById('demo-tri-enabled');
  var triRotationInput    = document.getElementById('demo-tri-rotation');
  var triRotationDisp     = document.getElementById('demo-tri-rotation-display');
  var triSizeInput        = document.getElementById('demo-tri-size');
  var triSizeDisp         = document.getElementById('demo-tri-size-display');
  var triWidthInput       = document.getElementById('demo-tri-width');
  var triWidthDisp        = document.getElementById('demo-tri-width-display');
  var centerCircleEnabledBtn = document.getElementById('demo-center-circle-enabled');
  var colorModeSel        = document.getElementById('demo-color-mode');
  var cosinePresetSel     = document.getElementById('demo-cosine-preset');
  var cosinePresetRow     = document.getElementById('demo-cosine-preset-row');
  var stopPresetSel       = document.getElementById('demo-stop-preset');
  var stopPresetRow       = document.getElementById('demo-stop-preset-row');
  var cosineHelpBtn       = document.getElementById('demo-cosine-help');
  var cosineModal         = document.getElementById('demo-cosine-modal');
  var cosineBackdrop      = document.getElementById('demo-cosine-backdrop');
  var cosineClose         = document.getElementById('demo-cosine-close');
  var paletteAInput       = document.getElementById('demo-palette-a');
  var paletteBInput       = document.getElementById('demo-palette-b');
  var paletteCInput       = document.getElementById('demo-palette-c');
  var paletteDInput       = document.getElementById('demo-palette-d');
  var paletteRows         = ['demo-palette-a-row','demo-palette-b-row','demo-palette-c-row','demo-palette-d-row'].map(function(id){ return document.getElementById(id); });
  var color0Input         = document.getElementById('demo-color0');
  var color1Input         = document.getElementById('demo-color1');
  var color2Input         = document.getElementById('demo-color2');
  var color3Input         = document.getElementById('demo-color3');
  var stopRows            = ['demo-color0-row','demo-color1-row','demo-color2-row','demo-color3-row'].map(function(id){ return document.getElementById(id); });
  var enableTextBtn       = document.getElementById('demo-enable-text');
  var textInput           = document.getElementById('demo-text');
  var textFontSel         = document.getElementById('demo-text-font');
  var textSizeInput       = document.getElementById('demo-text-size');
  var textSizeDisp        = document.getElementById('demo-text-size-display');
  var textXInput          = document.getElementById('demo-text-x');
  var textXDisp           = document.getElementById('demo-text-x-display');
  var textYInput          = document.getElementById('demo-text-y');
  var textYDisp           = document.getElementById('demo-text-y-display');
  var useTextColorBtn     = document.getElementById('demo-use-text-color');
  var textColorInput      = document.getElementById('demo-text-color');
  var textColorRow        = document.getElementById('demo-text-color-row');
  var outlineBtn          = document.getElementById('demo-outline-enabled');
  var outlineWidthInput   = document.getElementById('demo-outline-width');
  var outlineWidthDisp    = document.getElementById('demo-outline-width-display');
  var outlineWidthRow     = document.getElementById('demo-outline-width-row');
  var outlineColorInput   = document.getElementById('demo-outline-color');
  var outlineColorRow     = document.getElementById('demo-outline-color-row');
  var previewBtn          = document.getElementById('demo-preview-btn');
  var mockupModal         = document.getElementById('demo-mockup-modal');
  var mockupBackdrop      = document.getElementById('demo-mockup-backdrop');
  var mockupClose         = document.getElementById('demo-mockup-close');
  var mockupLoading       = document.getElementById('demo-mockup-loading');
  var mockupImg           = document.getElementById('demo-mockup-img');
  var mockupError         = document.getElementById('demo-mockup-error');
  var mockupFooter        = document.getElementById('demo-mockup-footer');
  var mockupSizeEl        = document.getElementById('demo-mockup-size');
  var mockupDownload      = document.getElementById('demo-mockup-download');
  var mockupOrder         = document.getElementById('demo-mockup-order');

  // ── Cycle dots ─────────────────────────────────────────────────────────────
  CYCLE_STEPS.forEach(function () {
    var dot = document.createElement('span');
    dot.className = 'shader-demo__cycle-dot';
    dotsWrap.appendChild(dot);
  });
  var dots = dotsWrap.querySelectorAll('.shader-demo__cycle-dot');

  function setActiveDot(index) {
    dots.forEach(function (d, i) {
      d.classList.toggle('is-active', i === index);
    });
  }

  // ── Apply helpers ──────────────────────────────────────────────────────────
  function setMode(mode) {
    window._demoState.values.u_color_mode = mode;
  }

  function applyPreset(name) {
    var p = PRESETS[name];
    if (!p) return;
    var v = window._demoState.values;
    setMode(p.mode);
    if (p.mode === 0) {
      v.u_palette_a = p.a; v.u_palette_b = p.b; v.u_palette_c = p.c; v.u_palette_d = p.d;
      if (paletteAInput) paletteAInput.value = rgbToHex(p.a);
      if (paletteBInput) paletteBInput.value = rgbToHex(p.b);
      if (paletteCInput) paletteCInput.value = rgbToHex(p.c);
      if (paletteDInput) paletteDInput.value = rgbToHex(p.d);
    } else {
      v.u_color0 = p.c0; v.u_color1 = p.c1;
      v.u_color2 = p.c2; v.u_color3 = p.c3;
      if (color0Input) color0Input.value = rgbToHex(p.c0);
      if (color1Input) color1Input.value = rgbToHex(p.c1);
      if (color2Input) color2Input.value = rgbToHex(p.c2);
      if (color3Input) color3Input.value = rgbToHex(p.c3);
    }
    if (colorModeSel) {
      colorModeSel.value = String(p.mode);
      applyColorModeVisibility();
    }
    presetSel.value = name;
    if (p.mode === 0 && cosinePresetSel) cosinePresetSel.value = name;
    if (p.mode === 1 && stopPresetSel)   stopPresetSel.value   = name;
    window._demoState.dirty = true;
  }

  function applyLineCount(n) {
    window._demoState.values.u_line_count = n;
    lineCountInput.value         = n;
    lineCountDisp.textContent    = n;
    window._demoState.dirty = true;
  }

  function applyPower(p) {
    window._demoState.values.u_power = p;
    powerInput.value      = p;
    powerDisp.textContent = p;
    window._demoState.dirty = true;
  }

  function applyCenterCircle(r) {
    window._demoState.values.u_center_circle_radius = r;
    centerCircleInput.value      = r;
    centerCircleDisp.textContent = r;
    window._demoState.dirty = true;
  }

  // ── Auto-cycle ─────────────────────────────────────────────────────────────
  var cycleIndex    = 0;
  var cycleTimer    = null;
  var userInteracted = false;

  function applyStep(index) {
    var step = CYCLE_STEPS[index];
    applyPreset(step.preset);
    applyLineCount(step.lineCount);
    applyPower(step.power);
    applyCenterCircle(step.centerCircle);
    setActiveDot(index);
  }

  function startCycle() {
    applyStep(0);
    cycleTimer = setInterval(function () {
      cycleIndex = (cycleIndex + 1) % CYCLE_STEPS.length;
      applyStep(cycleIndex);
    }, CYCLE_INTERVAL_MS);
  }

  function stopCycle() {
    if (cycleTimer) { clearInterval(cycleTimer); cycleTimer = null; }
    dotsWrap.style.display = 'none';
  }

  function hexToRgb(hex) {
    return [
      parseInt(hex.slice(1, 3), 16) / 255,
      parseInt(hex.slice(3, 5), 16) / 255,
      parseInt(hex.slice(5, 7), 16) / 255,
    ];
  }

  function rgbToHex(arr) {
    return '#' + arr.map(function (x) {
      var b = Math.round(Math.max(0, Math.min(1, x)) * 255).toString(16);
      return b.length === 1 ? '0' + b : b;
    }).join('');
  }

  function encodeState(values) {
    var out = {};
    Object.keys(values).forEach(function (k) {
      var v = values[k];
      out[k] = Array.isArray(v) ? rgbToHex(v) : v;
    });
    return btoa(JSON.stringify(out)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  }

  function decodeState(b64) {
    var padded = b64.replace(/-/g, '+').replace(/_/g, '/');
    while (padded.length % 4) padded += '=';
    var obj = JSON.parse(atob(padded));
    var result = {};
    Object.keys(obj).forEach(function (k) {
      var v = obj[k];
      result[k] = (typeof v === 'string' && v[0] === '#') ? hexToRgb(v) : v;
    });
    return result;
  }

  function applyColorModeVisibility() {
    var is4Color = colorModeSel && colorModeSel.value === '1';
    paletteRows.forEach(function (r) { if (r) r.style.display = is4Color ? 'none' : 'grid'; });
    stopRows.forEach(function (r)    { if (r) r.style.display = is4Color ? 'grid' : 'none'; });
    if (cosinePresetRow) cosinePresetRow.style.display = is4Color ? 'none' : 'grid';
    if (stopPresetRow)   stopPresetRow.style.display   = is4Color ? 'grid' : 'none';
  }

  function onUserInteract() {
    if (userInteracted) return;
    userInteracted = true;
    stopCycle();
  }

  // ── All Settings toggle ────────────────────────────────────────────────────
  if (allSettingsToggle && allSettingsBody) {
    allSettingsToggle.addEventListener('click', function () {
      var isOpen = this.getAttribute('aria-expanded') === 'true';
      this.setAttribute('aria-expanded', isOpen ? 'false' : 'true');
      allSettingsBody.hidden = isOpen;
    });
  }

  // ── Cosine help modal ──────────────────────────────────────────────────────
  function openCosineModal()  { if (cosineModal) cosineModal.classList.remove('shader-demo__help-modal--hidden'); }
  function closeCosineModal() { if (cosineModal) cosineModal.classList.add('shader-demo__help-modal--hidden'); }
  if (cosineHelpBtn)  cosineHelpBtn.addEventListener('click', openCosineModal);
  if (cosineClose)    cosineClose.addEventListener('click', closeCosineModal);
  if (cosineBackdrop) cosineBackdrop.addEventListener('click', closeCosineModal);

  // ── All Settings control wiring ────────────────────────────────────────────
  radiusInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.u_radius = n;
    radiusDisp.textContent = n;
    window._demoState.dirty = true;
  });

  widthTopInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.u_width_top = n;
    widthTopDisp.textContent = n;
    window._demoState.dirty = true;
  });

  widthBotInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.u_width_bot = n;
    widthBotDisp.textContent = n;
    window._demoState.dirty = true;
  });

  triEnabledBtn.addEventListener('click', function () {
    onUserInteract();
    var isOn = this.dataset.on === '1';
    var next = isOn ? 0 : 1;
    window._demoState.values.u_tri_enabled = next;
    this.dataset.on  = next ? '1' : '0';
    this.textContent = next ? 'On' : 'Off';
    this.classList.toggle('is-on', !!next);
    window._demoState.dirty = true;
  });

  triRotationInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseInt(this.value, 10);
    window._demoState.values.u_tri_rotation = n;
    triRotationDisp.textContent = n;
    window._demoState.dirty = true;
  });

  triSizeInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.u_tri_size = n;
    triSizeDisp.textContent = n;
    window._demoState.dirty = true;
  });

  triWidthInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseInt(this.value, 10);
    window._demoState.values.u_tri_width = n;
    triWidthDisp.textContent = n;
    window._demoState.dirty = true;
  });

  centerCircleEnabledBtn.addEventListener('click', function () {
    onUserInteract();
    var isOn = this.dataset.on === '1';
    var next = isOn ? 0 : 1;
    window._demoState.values.u_center_circle_enabled = next;
    this.dataset.on  = next ? '1' : '0';
    this.textContent = next ? 'On' : 'Off';
    this.classList.toggle('is-on', !!next);
    window._demoState.dirty = true;
  });

  colorModeSel.addEventListener('change', function () {
    onUserInteract();
    window._demoState.values.u_color_mode = parseInt(this.value, 10);
    applyColorModeVisibility();
    window._demoState.dirty = true;
  });

  if (cosinePresetSel) cosinePresetSel.addEventListener('change', function () {
    onUserInteract();
    applyPreset(this.value);
  });

  if (stopPresetSel) stopPresetSel.addEventListener('change', function () {
    onUserInteract();
    applyPreset(this.value);
  });

  paletteAInput.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_palette_a = hexToRgb(this.value);
    window._demoState.dirty = true;
  });
  paletteBInput.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_palette_b = hexToRgb(this.value);
    window._demoState.dirty = true;
  });
  paletteCInput.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_palette_c = hexToRgb(this.value);
    window._demoState.dirty = true;
  });
  paletteDInput.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_palette_d = hexToRgb(this.value);
    window._demoState.dirty = true;
  });

  color0Input.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_color0 = hexToRgb(this.value);
    window._demoState.dirty = true;
  });
  color1Input.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_color1 = hexToRgb(this.value);
    window._demoState.dirty = true;
  });
  color2Input.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_color2 = hexToRgb(this.value);
    window._demoState.dirty = true;
  });
  color3Input.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_color3 = hexToRgb(this.value);
    window._demoState.dirty = true;
  });

  // ── Control wiring ─────────────────────────────────────────────────────────
  presetSel.addEventListener('change', function () {
    onUserInteract();
    applyPreset(this.value);
  });

  lineCountInput.addEventListener('input', function () {
    onUserInteract();
    applyLineCount(parseInt(this.value, 10));
  });

  powerInput.addEventListener('input', function () {
    onUserInteract();
    applyPower(parseFloat(this.value));
  });

  centerCircleInput.addEventListener('input', function () {
    onUserInteract();
    applyCenterCircle(parseFloat(this.value));
  });

  randomizeBtn.addEventListener('click', function () {
    onUserInteract();
    var keys     = Object.keys(PRESETS);
    var name     = keys[Math.floor(Math.random() * keys.length)];
    var lineCount = Math.floor(Math.random() * 56) + 5;  // 5–60
    var power     = parseFloat((Math.random() * 5.5 + 0.5).toFixed(1)); // 0.5–6.0
    var centerCircle = parseFloat((Math.random() * 0.25 + 0.01).toFixed(3)); // 0.01–0.26
    applyPreset(name);
    applyLineCount(lineCount);
    applyPower(power);
    applyCenterCircle(centerCircle);
  });

  // ── Text overlay wiring ────────────────────────────────────────────────────
  function isTextEnabled() {
    return enableTextBtn && enableTextBtn.dataset.on === '1';
  }

  if (enableTextBtn) {
    enableTextBtn.addEventListener('click', function () {
      onUserInteract();
      var wasOn = this.dataset.on === '1';
      this.dataset.on  = wasOn ? '0' : '1';
      this.textContent = wasOn ? 'Off' : 'On';
      this.classList.toggle('is-on', !wasOn);
      window._demoState.values.text = wasOn ? '' : textInput.value;
      window._demoState.dirty = true;
    });
  }

  textInput.addEventListener('input', function () {
    onUserInteract();
    if (!isTextEnabled()) return;
    window._demoState.values.text = this.value;
    window._demoState.dirty = true;
  });

  textFontSel.addEventListener('change', function () {
    onUserInteract();
    window._demoState.values.textFont = this.value;
    window._demoState.dirty = true;
  });

  textSizeInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseInt(this.value, 10);
    window._demoState.values.textFontSize = n;
    textSizeDisp.textContent = n;
    window._demoState.dirty = true;
  });

  textXInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.textX = n;
    textXDisp.textContent = n;
    window._demoState.dirty = true;
  });

  textYInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.textY = n;
    textYDisp.textContent = n;
    window._demoState.dirty = true;
  });

  useTextColorBtn.addEventListener('click', function () {
    onUserInteract();
    var isOn = this.dataset.on === '1';
    var next = isOn ? 0 : 1;
    window._demoState.values.u_use_text_color = next;
    this.dataset.on  = next ? '1' : '0';
    this.textContent = next ? 'On' : 'Off';
    this.classList.toggle('is-on', !!next);
    if (textColorRow) textColorRow.style.display = next ? 'grid' : 'none';
    window._demoState.dirty = true;
  });

  textColorInput.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_text_color = hexToRgb(this.value);
    window._demoState.dirty = true;
  });

  outlineBtn.addEventListener('click', function () {
    onUserInteract();
    var isOn = this.dataset.on === '1';
    var next = isOn ? 0 : 1;
    window._demoState.values.outlineEnabled = !!next;
    this.dataset.on  = next ? '1' : '0';
    this.textContent = next ? 'On' : 'Off';
    this.classList.toggle('is-on', !!next);
    if (outlineWidthRow) outlineWidthRow.style.display = next ? 'grid' : 'none';
    if (outlineColorRow) outlineColorRow.style.display = next ? 'grid' : 'none';
    window._demoState.dirty = true;
  });

  outlineWidthInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseInt(this.value, 10);
    window._demoState.values.outlineWidth = n;
    outlineWidthDisp.textContent = n;
    window._demoState.dirty = true;
  });

  outlineColorInput.addEventListener('input', function () {
    onUserInteract();
    window._demoState.values.u_outline_color = hexToRgb(this.value);
    window._demoState.dirty = true;
  });

  // ── Finish controls ────────────────────────────────────────────────────────
  var opacityInput        = document.getElementById('demo-opacity');
  var opacityDisp         = document.getElementById('demo-opacity-display');
  var distressInput       = document.getElementById('demo-distress');
  var distressDisp        = document.getElementById('demo-distress-display');
  var distressScaleInput  = document.getElementById('demo-distress-scale');
  var distressScaleDisp   = document.getElementById('demo-distress-scale-display');

  opacityInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.u_opacity = n;
    opacityDisp.textContent = n.toFixed(2);
    window._demoState.dirty = true;
  });
  distressInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseFloat(this.value);
    window._demoState.values.u_distress = n;
    distressDisp.textContent = n.toFixed(2);
    window._demoState.dirty = true;
  });
  distressScaleInput.addEventListener('input', function () {
    onUserInteract();
    var n = parseInt(this.value, 10);
    window._demoState.values.u_distress_scale = n;
    distressScaleDisp.textContent = n;
    window._demoState.dirty = true;
  });

  // ── Preview on Shirt ───────────────────────────────────────────────────────
  var MOCKUP_WORKER      = 'https://brightfield-mockup-worker.eric-d-johnson.workers.dev/generate-mockup';
  var MOCKUP_VARIANT_ID  = 4017;
  var PRODUCT_HANDLE     = {{ section.settings.product_handle | default: 'line-circle' | json }};
  var currentDesignUrl   = null;
  var variantsLoaded     = false;

  function loadVariants() {
    if (variantsLoaded) return;
    variantsLoaded = true;
    fetch('/products/' + PRODUCT_HANDLE + '.js')
      .then(function (r) { return r.json(); })
      .then(function (product) {
        var sel = document.getElementById('demo-mockup-size');
        if (!sel) return;
        sel.innerHTML = '';
        product.variants.forEach(function (v) {
          var opt       = document.createElement('option');
          opt.value     = v.id;
          opt.textContent = v.title;
          if (!v.available) opt.disabled = true;
          sel.appendChild(opt);
        });
        // Hide size row if only one variant
        var sizeWrap = document.getElementById('demo-mockup-size-wrap');
        if (sizeWrap && product.variants.length <= 1) sizeWrap.style.display = 'none';
      })
      .catch(function () {
        var sizeWrap = document.getElementById('demo-mockup-size-wrap');
        if (sizeWrap) sizeWrap.style.display = 'none';
      });
  }

  function openMockupModal() {
    mockupModal.classList.remove('mockup-modal--hidden');
    mockupLoading.style.display = '';
    mockupImg.style.display = 'none';
    mockupError.style.display = 'none';
    if (mockupFooter) mockupFooter.style.display = 'none';
    document.body.style.overflow = 'hidden';
  }

  function closeMockupModal() {
    mockupModal.classList.add('mockup-modal--hidden');
    document.body.style.overflow = '';
  }

  mockupClose.addEventListener('click', closeMockupModal);
  mockupBackdrop.addEventListener('click', closeMockupModal);
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') { closeMockupModal(); closeCosineModal(); }
  });

  previewBtn.addEventListener('click', function () {
    if (!window._demoExport) return;
    loadVariants();
    openMockupModal();
    window._demoExport(1800, 2400, function (base64png) {
      fetch(MOCKUP_WORKER, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ image: base64png, variant_id: MOCKUP_VARIANT_ID }),
      })
        .then(function (res) { return res.json(); })
        .then(function (data) {
          if (!data.mockup_url) throw new Error('No mockup_url in response');
          currentDesignUrl = data.design_url || null;
          mockupLoading.style.display = 'none';
          mockupImg.src = data.mockup_url;
          mockupImg.style.display = '';
          if (mockupDownload) mockupDownload.href = data.mockup_url;
          if (mockupFooter) mockupFooter.style.display = '';
        })
        .catch(function () {
          mockupLoading.style.display = 'none';
          mockupError.style.display = '';
        });
    });
  });

  if (mockupOrder) {
    mockupOrder.addEventListener('click', function () {
      if (!currentDesignUrl) {
        alert('Design URL not available. Try generating a new preview.');
        return;
      }
      var variantId = mockupSizeEl ? mockupSizeEl.value : null;
      if (!variantId) {
        alert('Could not determine variant. Please select a size and try again.');
        return;
      }
      mockupOrder.disabled    = true;
      mockupOrder.textContent = 'Adding…';
      fetch('/cart/add.js', {
        method:  'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          id:       Number(variantId),
          quantity: 1,
          properties: {
            '_design_url':   currentDesignUrl,
            '_mockup_url':   mockupImg.src,
            'Customization': 'Custom Shader Design'
          }
        })
      })
      .then(function (r) {
        if (!r.ok) return r.text().then(function (t) {
          var msg = 'Cart error (' + r.status + ')';
          try { msg = JSON.parse(t).description || msg; } catch (e) {}
          throw new Error(msg);
        });
        window.location.href = '/cart';
      })
      .catch(function (err) {
        alert('Could not add to cart: ' + err.message);
        mockupOrder.disabled    = false;
        mockupOrder.textContent = 'Order This Design';
      });
    });
  }

  // ── Copy Link ──────────────────────────────────────────────────────────────
  var copyLinkBtn = document.getElementById('demo-copy-link');
  copyLinkBtn.addEventListener('click', function () {
    var self = this;
    self.textContent = 'Saving\u2026';
    self.disabled = true;
    fetch('https://brightfield-mockup-worker.eric-d-johnson.workers.dev/save-shader-state', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ state: window._demoState.values }),
    })
    .then(function (r) { return r.json(); })
    .then(function (data) {
      var url = window.location.origin + window.location.pathname + '#share=' + data.id;
      history.replaceState(null, '', '#share=' + data.id);
      function showConfirm() {
        self.textContent = 'Copied!';
        self.disabled = false;
        setTimeout(function () { self.textContent = 'Copy Link'; }, 1800);
      }
      var ta = document.createElement('textarea');
      ta.value = url; ta.style.position = 'fixed'; ta.style.opacity = '0';
      document.body.appendChild(ta); ta.select();
      try { document.execCommand('copy'); } catch (e) {}
      document.body.removeChild(ta);
      showConfirm();
    })
    .catch(function () {
      self.textContent = 'Failed';
      setTimeout(function () { self.textContent = 'Copy Link'; self.disabled = false; }, 1800);
    });
  });

  // ── Restore state from URL hash — #share=<id> (worker) or legacy #s=<base64> ──
  (function () {
    var hash = window.location.hash;

    function applyDemoState(v) {
      if (presetSel)              presetSel.value = '';
      if (lineCountInput)       { lineCountInput.value      = v.u_line_count;           lineCountDisp.textContent        = v.u_line_count; }
      if (powerInput)           { powerInput.value          = v.u_power;                powerDisp.textContent            = v.u_power; }
      if (centerCircleInput)    { centerCircleInput.value   = v.u_center_circle_radius; centerCircleDisp.textContent     = v.u_center_circle_radius; }
      if (radiusInput)          { radiusInput.value         = v.u_radius;               radiusDisp.textContent           = v.u_radius; }
      if (widthTopInput)        { widthTopInput.value       = v.u_width_top;            widthTopDisp.textContent         = v.u_width_top; }
      if (widthBotInput)        { widthBotInput.value       = v.u_width_bot;            widthBotDisp.textContent         = v.u_width_bot; }
      if (triEnabledBtn)        { var triOn = !!v.u_tri_enabled; triEnabledBtn.dataset.on = triOn ? '1' : '0'; triEnabledBtn.textContent = triOn ? 'On' : 'Off'; triEnabledBtn.classList.toggle('is-on', triOn); }
      if (triRotationInput)     { triRotationInput.value    = v.u_tri_rotation;         triRotationDisp.textContent      = v.u_tri_rotation; }
      if (triSizeInput)         { triSizeInput.value        = v.u_tri_size;             triSizeDisp.textContent          = v.u_tri_size; }
      if (triWidthInput)        { triWidthInput.value       = v.u_tri_width;            triWidthDisp.textContent         = v.u_tri_width; }
      if (centerCircleEnabledBtn) { var ccOn = !!v.u_center_circle_enabled; centerCircleEnabledBtn.dataset.on = ccOn ? '1' : '0'; centerCircleEnabledBtn.textContent = ccOn ? 'On' : 'Off'; centerCircleEnabledBtn.classList.toggle('is-on', ccOn); }
      if (colorModeSel)           colorModeSel.value = v.u_color_mode;
      if (paletteAInput)          paletteAInput.value = rgbToHex(v.u_palette_a);
      if (paletteBInput)          paletteBInput.value = rgbToHex(v.u_palette_b);
      if (paletteCInput)          paletteCInput.value = rgbToHex(v.u_palette_c);
      if (paletteDInput)          paletteDInput.value = rgbToHex(v.u_palette_d);
      if (color0Input)            color0Input.value   = rgbToHex(v.u_color0);
      if (color1Input)            color1Input.value   = rgbToHex(v.u_color1);
      if (color2Input)            color2Input.value   = rgbToHex(v.u_color2);
      if (color3Input)            color3Input.value   = rgbToHex(v.u_color3);
      if (textInput)              textInput.value     = v.text || '';
      if (textFontSel)            textFontSel.value   = v.textFont || 'Oswald';
      if (textSizeInput)        { textSizeInput.value = v.textFontSize;                 textSizeDisp.textContent         = v.textFontSize; }
      if (textXInput)           { textXInput.value    = v.textX;                        textXDisp.textContent            = v.textX; }
      if (textYInput)           { textYInput.value    = v.textY;                        textYDisp.textContent            = v.textY; }
      if (enableTextBtn)        { var txtOn = !!v.text; enableTextBtn.dataset.on = txtOn ? '1' : '0'; enableTextBtn.textContent = txtOn ? 'On' : 'Off'; enableTextBtn.classList.toggle('is-on', txtOn); }
      if (useTextColorBtn)      { var utcOn = !!v.u_use_text_color; useTextColorBtn.dataset.on = utcOn ? '1' : '0'; useTextColorBtn.textContent = utcOn ? 'On' : 'Off'; useTextColorBtn.classList.toggle('is-on', utcOn); if (textColorRow) textColorRow.style.display = utcOn ? 'grid' : 'none'; }
      if (textColorInput)         textColorInput.value = rgbToHex(v.u_text_color);
      if (outlineBtn)           { var olOn = !!v.outlineEnabled; outlineBtn.dataset.on = olOn ? '1' : '0'; outlineBtn.textContent = olOn ? 'On' : 'Off'; outlineBtn.classList.toggle('is-on', olOn); if (outlineWidthRow) outlineWidthRow.style.display = olOn ? 'grid' : 'none'; if (outlineColorRow) outlineColorRow.style.display = olOn ? 'grid' : 'none'; }
      if (outlineWidthInput)    { outlineWidthInput.value   = v.outlineWidth;           outlineWidthDisp.textContent     = v.outlineWidth; }
      if (outlineColorInput)      outlineColorInput.value   = rgbToHex(v.u_outline_color);
      if (opacityInput)         { opacityInput.value        = v.u_opacity;              opacityDisp.textContent          = parseFloat(v.u_opacity).toFixed(2); }
      if (distressInput)        { distressInput.value       = v.u_distress;             distressDisp.textContent         = parseFloat(v.u_distress).toFixed(2); }
      if (distressScaleInput)   { distressScaleInput.value  = v.u_distress_scale;       distressScaleDisp.textContent    = v.u_distress_scale; }
      window._demoState.textDirty = true;
      applyColorModeVisibility();
      userInteracted = true;
      stopCycle();
      history.replaceState(null, '', window.location.pathname + '#shader-demo');
    }

    // Legacy: inline base64
    if (hash.indexOf('#s=') === 0) {
      var encoded = new URLSearchParams(hash.slice(1)).get('s');
      if (encoded) {
        var restored;
        try { restored = decodeState(encoded); } catch (e) {}
        if (restored) {
          var v = window._demoState.values;
          Object.keys(restored).forEach(function (k) { v[k] = restored[k]; });
          applyDemoState(v);
        }
      }
      return;
    }

    // Short ID from worker
    if (hash.indexOf('#share=') === 0) {
      var shareId = hash.slice('#share='.length);
      fetch('https://brightfield-mockup-worker.eric-d-johnson.workers.dev/get-shader-state/' + shareId)
        .then(function (r) { return r.json(); })
        .then(function (state) {
          var v = window._demoState.values;
          Object.keys(state).forEach(function (k) {
            if (k in v) v[k] = (typeof state[k] === 'string' && state[k][0] === '#') ? hexToRgb(state[k]) : state[k];
          });
          applyDemoState(v);
        })
        .catch(function () { history.replaceState(null, '', window.location.pathname + '#shader-demo'); });
      return;
    }
  }());


  // ── Boot ───────────────────────────────────────────────────────────────────
  applyColorModeVisibility();
  if (!userInteracted) startCycle();
}());

// ── Desktop: lock controls height to canvas height ────────────────────────────
(function () {
  if (!window.ResizeObserver) return;
  if (window.innerWidth <= 800) return;
  var canvasWrap = document.querySelector('.shader-demo__canvas-wrap');
  var controls   = document.querySelector('.shader-demo__controls');
  if (!canvasWrap || !controls) return;
  new ResizeObserver(function (entries) {
    controls.style.maxHeight = entries[0].contentRect.height + 'px';
  }).observe(canvasWrap);
}());

// ── Desktop: scroll shadows on controls ───────────────────────────────────────
(function () {
  var el = document.querySelector('.shader-demo__controls');
  if (!el) return;
  function update() {
    el.classList.toggle('is-scrollable-top',    el.scrollTop > 4);
    el.classList.toggle('is-scrollable-bottom', el.scrollTop + el.clientHeight < el.scrollHeight - 4);
  }
  el.addEventListener('scroll', function onFirstScroll() {
    el.classList.add('has-scrolled');
    el.removeEventListener('scroll', onFirstScroll);
  }, { passive: true });
  el.addEventListener('scroll', update, { passive: true });
  if (window.ResizeObserver) { new ResizeObserver(update).observe(el); }
  update();
}());

// ── Mobile: fade controls as they scroll past the canvas bottom ───────────────
(function () {
  if (window.innerWidth > 800) return;
  var canvasWrap    = document.querySelector('.shader-demo__canvas-wrap');
  var controlsEl    = document.querySelector('.shader-demo__controls');
  var allSettingsEl = document.querySelector('.shader-demo__all-settings');
  if (!canvasWrap || !controlsEl) return;

  var FADE_ZONE = 72; // px — rows fade over this distance approaching canvas bottom

  function fadeRow(el, canvasBottom) {
    if (el.style.display === 'none') return;
    var rect    = el.getBoundingClientRect();
    var rowMid  = (rect.top + rect.bottom) / 2;
    var dist    = rowMid - canvasBottom;
    var opacity = dist <= 0 ? 0 : dist < FADE_ZONE ? dist / FADE_ZONE : 1;
    el.style.opacity       = opacity;
    el.style.pointerEvents = opacity < 0.15 ? 'none' : '';
  }

  function updateFade() {
    var canvasBottom = canvasWrap.getBoundingClientRect().bottom;
    var children = controlsEl.children;
    for (var i = 0; i < children.length; i++) {
      var child = children[i];
      if (child === allSettingsEl) {
        // Fade the toggle button as its own row
        var toggle = child.querySelector('.shader-demo__all-settings-toggle');
        if (toggle) fadeRow(toggle, canvasBottom);
        // Fade each row in the body individually when expanded
        var body = child.querySelector('.shader-demo__all-settings-body');
        if (body && !body.hidden) {
          for (var j = 0; j < body.children.length; j++) {
            fadeRow(body.children[j], canvasBottom);
          }
        }
      } else {
        fadeRow(child, canvasBottom);
      }
    }
  }

  window.addEventListener('scroll', updateFade, { passive: true });
  updateFade();
}());
</script>

<script src="{{ 'shader-base.js' | asset_url }}" defer></script>
<script src="{{ 'homepage-demo-shader.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Shader Demo",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow text",
      "default": "Make It Yours"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Every design is yours to customize"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "info": "Optional — leave blank to hide"
    },
    {
      "type": "text",
      "id": "product_handle",
      "label": "Product handle",
      "default": "line-circle",
      "info": "Used to load size options for ordering"
    },
    {
      "type": "url",
      "id": "product_url",
      "label": "Product page link",
      "info": "The Three-Square product page"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Buy Original Design"
    }
  ],
  "presets": [
    { "name": "Shader Demo" }
  ]
}
{% endschema %}
