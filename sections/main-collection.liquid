{%- paginate collection.products by 12 -%}

<div class="collection-page section-padding">
  <div class="container">

    <div class="collection-header">
      {%- assign collection_heading = collection.title -%}
      {%- if collection.handle == 'all' -%}
        {%- assign collection_heading = 'Shop' -%}
      {%- endif -%}
      <h1
        class="collection-title glitch"
        data-text="{{ collection_heading | escape }}"
      >
        {{ collection_heading | escape }}
      </h1>
      {%- if collection.description != blank -%}
        <div class="collection-description rte">{{ collection.description }}</div>
      {%- endif -%}
    </div>

    <div class="product-grid">
      {%- for product in collection.products -%}
        {% render 'product-card', product: product %}
      {%- else -%}
        <p class="empty-state">No products in this collection yet.</p>
      {%- endfor -%}
    </div>

    {%- if paginate.pages > 1 -%}
      <nav class="pagination" aria-label="Pagination">
        {{ paginate | default_pagination }}
      </nav>
    {%- endif -%}

  </div>
</div>

{%- endpaginate -%}

{% schema %}
{
  "name": "Collection",
  "settings": []
}
{% endschema %}
